# namespace :deploy do
#     before :starting, :ensure_user do
#         on roles(:all) do
#             execute "cd #{release_path} && bundle update marcel"
#           end
#         end
# end

# before :starting, :ensure_user
